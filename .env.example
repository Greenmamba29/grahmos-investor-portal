# ============================================================================
# GRAHMOS INVESTOR PORTAL - ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env to git - it contains secrets!

# ============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================================================
# Neon PostgreSQL connection string
# Get from: https://console.neon.tech → Connection Details
# Format: postgresql://username:password@host/database?sslmode=require
DATABASE_URL=postgresql://username:password@ep-xxxxx.region.aws.neon.tech/neondb?sslmode=require
NEON_DATABASE_URL=postgresql://username:password@ep-xxxxx.region.aws.neon.tech/neondb?sslmode=require

# ============================================================================
# AUTHENTICATION (REQUIRED)
# ============================================================================
# Secret key for JWT token signing - MUST be 32+ characters
# Generate with: openssl rand -base64 32
# Keep this secret and different for each environment!
SESSION_SECRET=your-strong-random-secret-key-min-32-characters

# Admin emails (comma-separated, NO SPACES, case-insensitive)
# These emails automatically receive admin role on signup
# Example: admin@grahmos.info,support@grahmos.info
ADMIN_EMAILS=admin@grahmos.info,support@grahmos.info

# ============================================================================
# APPLICATION URLS (REQUIRED)
# ============================================================================
# Your application domain - change for each deployment
# Local development:
VITE_APP_URL=http://localhost:8888
VITE_API_URL=http://localhost:8888/api
URL=http://localhost:8888

# Production examples:
# VITE_APP_URL=https://grahmos.info
# VITE_API_URL=https://grahmos.info/api
# URL=https://grahmos.info

# ============================================================================
# EMAIL SERVICE (OPTIONAL - for password reset & notifications)
# ============================================================================
# SendGrid (Recommended for production)
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
FROM_EMAIL=noreply@grahmos.info

# Alternative: Custom Webhook
WEBHOOK_URL=https://your-webhook-service.com/send-email
WEBHOOK_TOKEN=your_webhook_token

# ============================================================================
# DEVELOPMENT SETTINGS
# ============================================================================
NODE_ENV=development

# ============================================================================
# NOTES:
# ============================================================================
# 1. This project uses Neon PostgreSQL as the primary database
# 2. All auth functions (login, signup) use Neon, not Notion API
# 3. For multi-domain deployment, use the same DATABASE_URL but different URLs
# 4. See AUTH_SETUP_README.md for detailed setup instructions
# 5. See NOTION_TASKS.md for implementation tasks
